peer:
    mspName: KargaMSP
    peerName: hlf-peer--karga--peer9
    username: User1
    orgDomain: aptalkarga.tr
    peerAddress: hlf-peer--karga--peer9.default.svc
    channels:
      - common

splunk:
    hec:
        token: 12345678-ABCD-EFGH-IJKL-123456789012
        port: 8088
        host: splunk-splunk-kube.splunk.svc
    index: hyperledger_logs

secrets:
  peer:
    create: true
    # cert: hlf-peer--karga--peer9-msp
    # certItem: cert.pem
    # key: hlf-peer--karga--peer9-msp
    # keyItem: key.pem
